import React from 'react';
import {Text, View} from 'react-native';

const UserScreen = () => {
  return (
    <View style={{flex: 1, justifyContent: 'center', alignItems: 'center'}}>
      <Text>Insert user screen here</Text>
    </View>
  );
};

export default UserScreen;
